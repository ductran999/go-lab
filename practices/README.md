# Practices — how we build, not what we build

Research on development styles and trade-offs. Each doc follows
the house format: TL;DR → core idea → trade-offs → when to use.
Code labs live in `../storage/` and `../network/`; this folder
holds the methodology behind them.
